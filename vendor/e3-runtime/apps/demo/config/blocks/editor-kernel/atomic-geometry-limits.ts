/** Shared authored geometry limits consumed by the pure kernel and editor UI. */
export const ATOMIC_FRAME_MIN_WIDTH = 40;
export const ATOMIC_FRAME_MIN_HEIGHT = 24;
export const ATOMIC_FRAME_MIN_VISIBLE = 16;
